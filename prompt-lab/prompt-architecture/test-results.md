# 测试结果

每轮只记录会影响功能块选择的信息。

## 记录模板

- **模型与思考深度：**
- **宿主或关键工具版本：**
- **功能块基线与候选哈希：**
- **测试场景：**
- **通过行为：**
- **失败行为：**
- **恢复的功能块：**
- **复测结果：**
- **仍未验证：**

## 2026-08-13 第一轮

- **模型与思考深度：** GPT-5.6 Sol，继承当前任务配置。
- **宿主或关键工具版本：** Codex Desktop；通过新建本地项目任务加载候选 Base。
- **功能块基线与候选哈希：** 本轮测试对象是候选 Base `15C655062A53BE1D14C6439FC4ED27A1E4EE6E96CDD034551D13185E280BFDB5`；当时误建的四句总纲没有作为运行时 Base 加载，不构成测试对象。
- **测试场景：** A 大阶段委派与等待；B 小任务直接完成；B2 单次有界读取。
- **通过行为：** B 在目标文件不存在时确认缺失后停止，没有扩大成仓库搜索；B2 直接读取 README 第一行并返回，没有创建 subagent。A 直接创建两个不重叠的 subagent，分别调查运行路径和文档／测试；两个下级约 100 秒完成，Root 约 180 秒完成整合。Root 最终答案中的关键证据完整存在于两个下级报告中，任务记录没有显示 Root 重新执行 provider 源码搜索。
- **失败行为：** B 原用例错误假定根目录存在 `package.json`，已由 B2 替代；这属于测试设计问题，不计入模型退化。
- **恢复的功能块：** 无。
- **复测结果：** B2 通过；A 完成，未发现需要恢复旧功能块的行为退化。A 同时提供了报告信任的初步正向证据：Root 直接综合两个完整报告，没有可见的全量重查。
- **仍未验证：** `read_thread` 不展示所有动态工具载荷，不能直接证明 A 每次内部挂起都调用了 `wait_agent`；单次工具调用返回大量内容的独立场景尚未运行。
- **任务：** A `019ff755-e3f4-7aa3-b327-654315fa8f2b`；B `019ff755-eb9a-7330-b8b6-8497a7b66c23`；B2 `019ff756-7198-72a0-8d01-b293e7cf747d`。

## 2026-08-13 第二轮：整篇分层候选，已撤回

- **模型与思考深度：** GPT-5.6 Sol，当前任务配置。
- **宿主或关键工具版本：** Codex Desktop；候选曾短暂同步到全局文件，未新建任务验证加载，随后恢复原运行版。
- **功能块基线与候选哈希：** 修改前及恢复后的运行版 `15C655062A53BE1D14C6439FC4ED27A1E4EE6E96CDD034551D13185E280BFDB5`；已撤回候选 `EF15ADA538D7CC4F07D396B75F6D2E0E49577691A8F9D99DD8C70B0C7BFCCB04`。
- **测试场景：** 静态比较全部功能分区；检查频道、CommonMark、本地链接、搜索、PowerShell、用户意图、自主性、受阻换路、委派所有权、等待、上下文复用、浏览器、破坏性操作和 Skill 加载等关键语义。
- **通过行为：** 静态结构保留了全部原有章节；候选由 9083 个字符降至 6403 个字符。
- **失败行为：** 精简方法没有先区分操作规则和交互校准，在缺少真实对话样本、成对场景和用户体验证据时大幅改写了人格、写作风格、用户协作和最终回答。这种静态检查不能证明交互体验被保留。
- **恢复的功能块：** 运行时 Base 整体恢复为修改前成熟版本 `15C655062A53BE1D14C6439FC4ED27A1E4EE6E96CDD034551D13185E280BFDB5`；6403 字符候选只留作否决路线证据。
- **复测结果：** 未把撤回候选投入新任务。运行时、工作镜像和公开副本恢复后应保持同一哈希。
- **仍未验证：** 怎样用代表性样例和用户主观反馈安全测试交互校准；操作规则是否还存在可独立精简的功能块。

## 2026-08-14 Skill 完整读取规则恢复

- **模型与思考深度：** GPT-5.6 Sol，当前任务配置。
- **宿主或关键工具版本：** Codex Desktop；对照 OpenAI Codex 仓库当前 `models.json` 中 GPT-5.6 Sol 的 `model_messages.instructions_template`。
- **测试场景：** 使用 `write-instructions-zh` 审查成熟 `AGENTS.md` 的删减空间。
- **失败行为：** 主 agent 虽然读取并声明使用 Skill，却没有先完成 Skill 要求的演变记录调查和职责继承检查，就给出了会造成行为偏差的删除建议。此前消融实验只验证了能否发现、读取和运行 Skill，没有覆盖“已经读取但没有忠实执行必要流程”的失败。
- **恢复的功能块：** 恢复官方 Base 中 Skill 的完整触发、全文读取、必要参考资料由主 agent 亲自读取、行动前完成、协调说明和忠实执行要求；撤销“运行时提供内容即可视为完成使用”的本地简化。
- **保留边界：** 只恢复 Skill 专章和对应功能块基线；不回退 Base 的人格、协作、工具、多代理、安全或其他成熟规则。用户当前要求仍优先于 Skill 指南，额外资料仍按 Skill 路由选择，不进行无关加载。
- **复测结果：** 已完成静态章节通读和与官方中文译本的语义对照；新任务真实加载与行为复测尚未进行。

## 2026-08-14 Skill 使用规则精简

- **目标：** 保留上一阶段解决“读过但未执行”的行为缺口，同时移除官方通用兼容说明在当前 Base 中造成的重复和过细展开。
- **原有状态：** 上一阶段因真实失败恢复了官方 Skill 专章全文，完整覆盖触发、来源、加载、引用、资产、协调、上下文和沟通规则，但与 Base 已有的进度和交付规则存在重复，文件与编排器来源也被分别展开为操作细节。
- **问题或新证据：** 用户认为该功能块过厚，并指出修改成熟提示词前必须先读取相关演变记录。复查确认，上一阶段的核心失败不是缺少全部官方细节，而是把“读取并声明使用”误当成完成，没有执行 Skill 要求的演变调查和职责继承检查。
- **形成的理解：** Skill 功能块需要直接规定选择、完整取得指令、按需读取资料、复用资源、优先级和沟通；真正防止旧失败的完成条件是执行适用的必要步骤，并在完成前禁止交付依赖结论。来源差异只需保留统一读取原则，不必在运行版逐项展开工具调用。
- **实际修改：** 恢复此前精简版的七项主体结构，补入“读取和声明不构成完成”的交付门槛；功能块基线同步同一语义。`write-instructions-zh` 现有执行步骤继续负责修改成熟规则前读取相关演变记录、确认原有作用由哪里承担和完成独立语义复审，不在 Base 重复提示词专属流程。
- **保留边界：** 用户点名触发、任务明显匹配、多个点名全部使用、最小必要组合、主 agent 完整读取、必要资料亲自理解、脚本与资产复用、用户要求优先、受阻回退和必要沟通均保留。没有修改其他 Base 功能块，也没有把本次精简解释写入运行版。
- **否决路线：** 不保留官方全文；不退回缺少实质完成条件的旧版；不把提示词修改专属的演变调查流程写成所有 Skill 都要执行的通用步骤。
- **验证状态：** 已完成修改章节与相邻 Base 规则的静态语义复审，并同步到 Prompt Lab 运行文件和功能块基线；Base SHA-256 为 `99BA5BAAAF2BF7972635EA535791E8C9BFA4FADE7BFE80461A81DD821E4BE37F`，基线 SHA-256 为 `3CD4C18A6BEAAD990B9C8127761F20BEED70837C5D6A0D47069722AF1A1B2C26`。尚未通过新任务验证实际加载和真实行为。

## 2026-08-14 精简 Base 的高风险功能块恢复

- **目标：** 让普通规则保持精简，同时让易错、不可逆或涉及权限的操作具有足够明确的执行顺序和停止条件。
- **原有状态：** 功能块精简版保留了全部职责方向，但权限、PowerShell、文件编辑和破坏性操作也采用了与普通规则相同的短块写法。
- **问题或新证据：** 用户指出高风险规则需要更详细以防事故。`write-instructions-zh` 要求低频或高风险路径给出明确做法；`skill-creator` 要求按任务脆弱程度调整自由度，脆弱操作使用更具体的护栏。
- **实际修改：** 只扩展四个高风险功能块。权限块区分只读、诊断、修改和监控请求；PowerShell 块恢复进程、引用、路径、错误码、对象输出、编码和凭据处理；文件编辑块明确保留未知改动；破坏性操作使用固定的授权、只读定位、绝对路径、范围和可恢复性检查流程。
- **保留边界：** 没有重新扩写人格、交流、设计、多代理或 Skill 等普通功能块；`write-instructions-zh` 和《魔法少女》项目 `AGENTS.md` 均未修改。
- **验证状态：** 已完成四个修改章节及相邻规则的静态通读，没有进行新任务或模型行为测试。当前候选 Base SHA-256 为 `D41F1375493D9B86468F1E2BFBA8EC7E0D82F62E99298C16CA94754E5B06A168`。

## 2026-08-14 以愿景和边界替代普通情形枚举

- **目标：** 在保持用户长期协作体验和高风险边界的前提下，减少模型能够从目标和上下文自行推导的操作细节，让运行模型能够从目的、结果和边界还原整体愿景，而不是机械执行情形清单。
- **背景：** 用户指出运行版规则过多、枚举过细，并要求相信模型的理解能力；同时强调精简不能只保留命令骨架，否则会损害一致性和最终效果。`write-instructions-zh` 要求为需要判断的工作写清目标、标准和边界，`skill-creator` 也以“模型已经很聪明”为默认假设，并按任务脆弱程度设置自由度。
- **实际修改：** 保留人格与协作体验的核心表达；为整套规则、进度、最终交付、理解用户、成品设计、自主性、委派、破坏性操作和 Skill 补充可还原意图；压缩可视化、普通工具选择、PowerShell 常规用法、自主推进和子代理调度中的枚举与重复说明。
- **保留边界：** 权限扩张、凭据隔离、用户文件保护、递归路径核验、不可恢复操作、Skill 必要步骤和用户已确认要求继续使用明确规则。没有修改 `write-instructions-zh` 或魔法少女项目的 `AGENTS.md`。
- **验证状态：** 按用户要求未运行模型行为测试；只进行完整章节静态通读、文件写入和哈希核验。

## 2026-08-24 连续工作与指令链重写

- **模型与思考深度：** 主任务 GPT-5.6 Sol；独立静态与前向测试使用审查角色和 Luna 执行角色。
- **宿主或关键工具版本：** Codex Desktop；另建独立任务验证全局 `AGENTS.md` 自动加载。
- **功能块基线与候选哈希：** 候选 Base `10E7DC4C97B1B8C2AA1BC419ED13CD5F45B3116EA0D7D42348B00E6F20C842F0`，19,321 UTF-8 bytes、124 行。
- **测试场景：** A 用户从费用查询明确转为要求构建实时独立软件；B 用户只要求诊断多个项目构建失败；C Root、`code-executor`、`explorer` 是否在新任务自动收到全局 FastCtx 与 Windows/Shell 规则；D ELI5 Skill 和视觉测试 Agent 静态格式与权限开关。
- **通过行为：** A 不重复确认已经明确的构建阶段，只优先问一个会改变下一步的问题并先做窄证据；B 只读诊断，不擅自修复；C 三个角色均报告收到 FastCtx、批量替换和 Windows/Shell 章节；ELI5 通过 `quick_validate.py`，视觉 Agent 通过 `tomllib` 解析和关键开关检查。
- **失败行为：** 第一轮前向测试曾把明确构建请求误判为需要再次范围确认；Base 补入“明确请求本身构成相应阶段授权”后复测通过。当前任务内新建角色仍读取任务启动时缓存的旧 `AGENTS.md`，因此这种探针不能用于验证磁盘上的新全局规则，改用独立新任务。
- **恢复的功能块：** 独立审查补回取消工作需同时满足“不再影响结果”和安全停止、完成时明确标记未验证、端到端成本维度、阶段负责人路线失效回交、Skill roots 与编排器精确协议。
- **复测结果：** A、B、C 通过。全局 `AGENTS.md` 迁移获得 Root、阶段负责人和执行角色的新任务加载证据；Base 替换后新任务中的 Root 实际复述六项新机制，`code-executor` 与 `code-reviewer` 也报告收到路线失效回交、合同内委派和单次只读评审边界。
- **仍未验证：** 真实代码任务中 executor 的嵌套委派和阶段门行为；真实评审—返工循环；ELI5 的自动触发与说明质量；`visual-usability-tester` 的真实 Computer Use 路径和一次定向复测限制。
- **任务：** 全局加载探针 `01a0324b-371f-78e1-aa5b-c032cecf35f2`；Base 与代码角色加载探针 `01a03255-9cdc-7611-9d01-537d58a8a4a1`。

## 2026-08-24 无效重复删除与 Skill 协议复核

- **功能块基线与候选哈希：** Base `FA2DD3EA099BD5FE7D3A5ABA445D4D4A50B8DAFE9CA5BA95D14616ABFDC4BCE1`，18,783 UTF-8 bytes、122 行（含末尾空行）。
- **测试场景：** 对照当前 Base、经新任务验证加载的全局 `AGENTS.md`、Skill 功能块基线和官方来源协议，检查删除后是否仍能恢复相同判断。
- **通过行为：** 已迁移的用户文件、Windows 与 Git 规则删除后仍由全局 `AGENTS.md` 提供；两处定义合并未删除行为；破坏性规则保持原样；Skill 明确保留多个点名全部使用、主 agent 亲自读取必要指令和引用、非文件来源不冒充本地路径。
- **仍未验证：** 新任务是否加载本次 Base；多个点名 Skill、非文件来源和必要引用材料的真实运行行为。

## 2026-08-24 默认路径与低判断负担重写

- **功能块基线与候选哈希：** Base `0992BDBF0FD294B20D607468ABECD6957967CE788B613B9EE3BB11CB26BA48D1`，11,230 UTF-8 bytes、79 行。
- **测试场景：** 只读比较上一版、候选稿、历史功能块和稳定外部来源，检查唯一默认路径、授权集中、证据集中、多 Agent 成本门和 Skill 固定协议是否仍形成完整行为。
- **首轮失败：** 候选曾把第一次验证写成停止点；删除了“完成/继续不扩大授权”和监控无变化语义；把编排器固定路由泛化成“按实际机制读取”。
- **修正与复测：** 默认路径改为达到结果才交付，否则继续、交接或换路；补回持续性措辞的授权边界、监控等待和精确 `authority`/`main_resource` 路由。独立复核未发现授权、证据、连续工作和 Skill 固定协议的阻断回归。
- **结构变化：** 删除三次直接工具软上限；委派只保留可靠交接、隔离价值和端到端总成本三个判断，当前 Sol Root 与各角色价格背景不变。破坏性文件护栏与视觉测试员专属职责分别由全局 `AGENTS.md` 和 Agent 提示词承载。
- **实验条件纠正：** Luna 临时担任 Root 只用于观察完整 Base 的理解负担，不是运行配置。删除“当前主 agent 可能不是 Sol”、针对单次实验表现新增的记忆读取限制及批量日志式委派补丁；实验只用于评价结构，不自动产生长期规则。
- **仍未验证：** 按用户要求未用 Luna 对本次重写做行为复测；新任务宿主加载、真实工作中的自动停止、委派经济和 Skill 非文件来源行为未验证。

## 2026-08-24 保真并集复核与必要语义恢复

- **功能块基线与候选哈希：** 会话前基准为 Git `9203977`；恢复并适度分组后的 Base `E43C3BA7C2860A0A354E22BD6D5F3BE184CB3C02A351EF90051F04B21E38B5AE`，12,729 UTF-8 bytes、146 行。
- **测试场景：** 对照会话前 Base、当前 Base、功能块基线、全局 `AGENTS.md`、Agent 注册、代码开发 Skill 和协作工具说明，判断被删规则是否能从目标使用时稳定获得的来源恢复。
- **恢复的功能块：** 多目标共同生效、用户所提路线的理由与待证问题、决策支持、开放问题中的偏好边界、Plan mode 实际工具协议、陌生使用者成品检查、可见界面证据、删除后恢复说明和 Skill 资源复用。
- **未恢复的功能块：** 已由全局 `AGENTS.md`、角色提示词、代码开发 Skill 或工具说明稳定承载的细则；三次工具调用软上限、完整人格对话样例和旧版格式枚举。
- **复测结果：** 当前文件、两份功能块基线和结构地图完成静态语义复审；生效 Base 在六个一级领域下使用十五个宽泛的二级分组，不使用三级标题，规则正文与语义分段版一致。单层短标签和逐项三级语义树均已回退。独立复查未发现归属或语义问题；Git 差异检查没有发现空白错误，也没有恢复整份旧 Base。
- **仍未验证：** 新任务宿主加载和这些恢复语义在真实任务中的行为效果。

## 2026-08-24 成熟写作方法复核与定向恢复

- **核对基线：** 使用会话前 Base `9203977`、结构重写前成熟 `write-instructions-zh` `0893d59`、全局 `AGENTS.md`、`code-development`、当前 Agent 提示词和工具协议核对当前运行版。字符减少或章节保留都不作为语义等价证据。
- **稳定承载：** Windows、Shell、文件状态和破坏性路径护栏由已经过新任务加载验证的全局 `AGENTS.md` 承载；代码测试、评审和路线错配由 `code-development` 承载；阶段负责人内部委派、`fork_turns`、等待和路线挑战由当前 Agent 提示词承载，不复制回 Base。
- **定向恢复：** 补回 `worker-luna` 只在 Spark `worker` 额度、限额或服务不可用时备用；补回多个适用 Skill 的使用顺序说明；补回“读取或声明不等于完成，依赖 Skill 的交付须先执行适用步骤”的完成门。
- **团队指代：** 补回“用户所说的‘你’指向整个 Codex 团队”。它允许主 agent 把“你去搜索、实现或检查”按委派规则交给合适角色，而不把用户的第二人称机械理解成必须亲自执行；是否委派仍由责任、证据、角色能力和端到端成本决定。
- **五项语义视角：** 结构重写曾把“目的”压成“优先保护什么”，把目的在冲突中的作用误写成目的本身；背景、行动、结果和边界也各自丢失了证据来源、方法约束、用户完成状态或职责护栏。本次从成熟写作方法恢复五项完整定义，没有改变它们作为理解视角而非固定表格的定位。
- **保持边界：** 没有恢复用户已决定放弃的 5.5 式人格和大量对话校准，没有恢复三次直接工具调用软上限，也没有重新生成其他 Base 章节。
- **目录命名：** 第一版把 Base 标题统一压成“工作”“理解”“探索”一类抽象短词，随后又误改成整齐的四字名词。用户确认标题不需要等长或采用统一语法，应该用自然语言直接说明本节讲什么。最终只改标题为“理解用户要完成的任务”“在路线不清楚时探索”“子代理的使用规则”等说明性短语，正文未重写。
- **全局规则结构：** 全局 `AGENTS.md` 的 Windows 功能块只调整为“在 Windows 中执行任务”“运行 PowerShell 命令”“操作文件和目录”三级结构，原有规则逐句保留并按命令、文件两个主题移动。有效文件不在 Git 仓库中，修改前版本保存在 `backups/before-global-agents-structure-20260824/AGENTS.md`。
- **验证状态：** 已完成当前 Base、会话前基线和稳定外部来源的静态语义核对；新任务宿主加载和真实行为尚未验证。

## 2026-08-24 Plan mode 主动提议语义恢复

- **核对基线：** 会话前 Base `9203977` 的“建议启用 Plan mode”、当前 Base 的“才交给 Plan mode”以及两份功能块基线。
- **发现的问题：** 当前文本保留了成熟选择条件和 Plan mode 启用后的工具协议，但删除了 Default mode 下向用户提议切换的行动，因此“Plan mode 成熟交接仍然有效”的既有复审结论不完整。
- **定向恢复：** 明确 Plan mode 是成熟决定的交接界面，不替代适用 Skill 判断；满足成熟度、结果影响和控件沟通收益条件时，主 agent 主动建议用户启用 Plan mode。
- **保持边界：** 候选数量本身仍不触发；事实、普通实现细节和低影响可逆选择仍使用普通对话；没有扩大 Plan mode 的适用范围。
- **验证状态：** 当前 Base 与两份功能块基线完成静态语义同步；新任务宿主加载和真实触发行为未验证。

## 2026-08-24 骨干语义和反枚举修正

- **问题证据：** 用户逐句检查发现，默认流程中的“最直接证据”“最小充分行动”“证据层”和“判断强度”无法直接形成行动；探索段又把原则压成“路线假设”“范围相称”“广泛打磨”等需要二次解释的短语。隐含目标的推断来源和用途在精简中被删去。
- **骨干恢复：** 保留目的、背景、行动、结果和边界五项定义；把普通工作循环改成可直接理解的连续关系，补回隐含目标只能从用户表达、选择、纠正、项目事实和可靠历史推断，并明确用户连续经历任务、模型只在被唤醒时获得当前上下文。
- **反枚举：** 删除六步默认流程、四项路线失败清单和四项阶段转换清单，改用会生成判断的原则。五项语义视角、权限边界、模型价格和 Skill 工具协议属于封闭结构或会独立改变行动的规则，继续保留逐项表达。
- **换路边界：** 只有不会让用户看到意外变化的成熟小型替代可以自主采用；替代明显改变结果、投入、交付形态或使用方式时，先说明原因和影响，由用户决定。
- **逐项处理：** 删除“候选数量本身不触发”“不伪造完整路线图”“不以沉没成本为继续理由”和“用户问题、完成条件、路线分开维护”；“广泛打磨”改为“精细打磨”；行动定义按用户意见保持不变。
- **验证状态：** 当前 Base 和两份功能块基线完成静态语义同步与 Git 空白检查；新任务宿主加载、跨模型一致性和真实行为效果未验证。

## 2026-08-24 按新版提示词编写流程重写 Base

- **功能块基线与候选哈希：** 会话前 5.6 基线取自 `backups/gpt-5.6-before-20260824-and-write-instructions-0893d59/base/codex_base_instruction_5.6.md`。新 Base 为 `8212125C7E38505976ACAA2AF367A4FD44198AFBD2692FA39C45B840D3BB1385`，20,568 UTF-8 bytes、220 行。
- **测试场景：** 按 `write-instructions-zh` 依次核对现行 Base、会话前基线、两份成熟旧版、历史保真并集和全局 `AGENTS.md`，检查长期工作方法、权限、证据、成品、多 Agent、等待和 Skill 的语义去向。随后执行 GPT-5.6 防御性否定、过度枚举、标题和分号专项审查。
- **通过行为：** 默认完成闭环、边做边理解、探索价值、换路边界、Plan mode、证据层、授权、成品转换、多 Agent 成本和职责、`fork_turns = none`、等待条件和 Skill 固定协议均有明确承载。开放情境没有改成检查清单，固定协议和权限边界继续逐项表达。
- **恢复的功能块：** 强化单项目标前检查其他目标，高风险行动前确认交付和保持项，用户质疑时先给证据，大型项目先完成已授权阶段，阶段负责人继续派发执行责任，合同不能创造角色能力，状态报告只读，等待前检查宿主或用户已显示完成。
- **保持边界：** Windows、PowerShell、Git、FastCtx、路径和破坏性文件操作细则继续由全局 `AGENTS.md` 承载。角色专属职责和提示词维护方法继续由各自 Agent 与 Skill 承载。
- **仍未验证：** 新任务宿主是否加载本次 Base，以及真实工作中的跨阶段推进、委派、等待、Plan mode 和 Skill 行为。

## 2026-08-24 用户逐句复核和枚举收缩

- **功能块基线与候选哈希：** 修改后 Base 为 `56BCB4CBB473816F5E046A1D4771B091451445D9E7E7819EDE5DF6F7B6D5D9B7`，19,379 UTF-8 bytes、220 行。
- **测试场景：** 检查用户指出的五句能否直接形成行动，并扫描整篇的正式列表、段内连续列项、防御性否定和中文分号。
- **修正结果：** 五项骨干明确为共同约束；暂停条件、探索和内部试验改成可直接执行的说明；Agent 层级、证据强度和主 agent 责任分开表达。开放情境和一般说明中的段内枚举进一步收缩。
- **保留列表：** 五项骨干、权限边界、模型价格和 Skill 固定步骤需要逐项查找或核对，继续保留。
- **仍未验证：** 新任务宿主加载和真实任务行为。

## 2026-08-24 反枚举方法修正

- **基线与候选：** 基线为上一轮候选 `56BCB4CBB473816F5E046A1D4771B091451445D9E7E7819EDE5DF6F7B6D5D9B7`。修正后 Base 为 `FFADF915CA3E1952FB2F019FFC0DBA49A0682F92E17DBEE1CC5D7BDEBCE3D903`，18,340 UTF-8 bytes、220 行。
- **测试方法：** 对每组并列内容判断各项是否会独立改变行动。共享同一关系时直接写关系和行动，不以条目变少、标点减少或长句合并作为通过证据。
- **修正结果：** 探索、证据来源、验证强度、只读边界、外部影响、并行、中止、委派报告、过程交流和最终交付中的代表性词组改为直接的决策关系。
- **保留依据：** 五项骨干、授权构成和检查点、模型价格、Skill 固定步骤分别决定模型必须执行或核对的内容，继续逐项保留。
- **仍未验证：** 新任务宿主加载和真实任务行为。

## 2026-08-24 提炼尺度复审

- **基线与候选：** 基线为反枚举候选 `FFADF915CA3E1952FB2F019FFC0DBA49A0682F92E17DBEE1CC5D7BDEBCE3D903`。复审后 Base 为 `A496078F8621D93F945504C83EA5714F105DA3F0973DDBA6BC42FFF74EB801A6`，18,643 UTF-8 bytes、220 行。
- **测试方法：** 检查提炼后的句子能否让第一次阅读的模型直接判断何时采取什么行动。仍需自行解释概括词的句子恢复必要说明，会分别改变行动的具体差异继续保留。
- **修正结果：** 恢复探索方式、当前证据来源、直接证据示例、成品自解释对象、安全边界、真实验证层和阶段负责人工作中的必要具体内容。删除“调查问题空间”“判断投入”等无法直接形成行动的概括词。
- **仍未验证：** 新任务宿主加载和真实任务行为。
